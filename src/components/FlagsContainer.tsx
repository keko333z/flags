/*import { Flag } from "./Flag";
import { Country } from "../App";

interface Props {
    countries: Array<Country>
}

export function FlagContainer({countries}: Props){
    return (
        <div className="flags-countainer">{countries.map((c: Country)=><Flag country={c} />)}</div>
    )
}*/